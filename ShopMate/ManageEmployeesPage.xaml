<Page
    x:Class="ShopMate.Pages.ManageEmployeesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ShopMate.Pages"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <ScrollViewer>
        <StackPanel Padding="40" Spacing="20">

            <!-- Back -->
            <Button Content="← Back to Dashboard" Click="OnBackClicked" HorizontalAlignment="Left"/>

            <!-- Header -->
            <Border Background="#3B82F6" CornerRadius="10" Padding="20">
                <TextBlock Text="Manage Employees" FontSize="28" FontWeight="Bold" Foreground="White"/>
            </Border>

            <!-- Add Employee Form -->
            <StackPanel Spacing="10" MaxWidth="500">
                <TextBlock Text="Add New Salesperson" FontSize="20" FontWeight="SemiBold"/>
                <TextBox Header="Name" x:Name="NameTextBox"/>
                <TextBox Header="Email" x:Name="EmailTextBox"/>
                <TextBox Header="Phone" x:Name="PhoneTextBox"/>
                <Button Content="Add Employee" Click="AddEmployee_Click"/>
            </StackPanel>

            <!-- Employees List -->
            <TextBlock Text="Existing Employees" FontSize="20" FontWeight="SemiBold"/>
            <ListView x:Name="EmployeesListView" SelectionMode="None">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="local:Employee">
                        <Border CornerRadius="8" Padding="10" Margin="0,5" Background="White">
                            <Grid ColumnDefinitions="*,*,*,Auto" VerticalAlignment="Center">
                                <TextBlock Text="{x:Bind Name}" Grid.Column="0"/>
                                <TextBlock Text="{x:Bind Email}" Grid.Column="1"/>
                                <TextBlock Text="{x:Bind Phone}" Grid.Column="2"/>
                                <Button Content="Remove" Click="RemoveEmployee_Click" Grid.Column="3" Tag="{x:Bind Id}" Background="#FEE2E2" Foreground="#B91C1C"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

        </StackPanel>
    </ScrollViewer>
</Page>
